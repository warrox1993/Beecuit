# Phase 4C — Products & Listings — Plan

## Tasks

- [ ] **T1** — i18n strings (4 locales) : `shop.coffretIncludes`
- [ ] **T2** — Extend `listCoffretsForLocale` to expose `breakdownNames: string[]` (3 first)
- [ ] **T3** — Rewrite `ProductCard.tsx` (premium frame + hover + price cartouche)
- [ ] **T4** — Rewrite `CoffretCard.tsx` (gift-box visual + composition reveal + ruban)
- [ ] **T5** — Restyle `CategoryFilter.tsx` (pills honey-dark/cream sidebar + chips)
- [ ] **T6** — Validation tsc/lint/vitest/build/e2e + memory update

## Commits

1. `feat(phase4C): add shop.coffretIncludes i18n + listCoffretsForLocale breakdownNames`
2. `feat(phase4C): rewrite ProductCard with premium frame, hover gold, price cartouche`
3. `feat(phase4C): rewrite CoffretCard with composition reveal + gold ribbon`
4. `feat(phase4C): restyle CategoryFilter as honey-cream pills`
5. `docs(phase4C): memory update`
